# 临时/调试产物
.tmp_*
__check_*
__pycache__/
.DS_Store

# APK 签名密码文件（勿入库）
.ks_pass

# 依赖与 IDE/AI 工作目录（不入库）
node_modules/
.trae/
.workbuddy/

# 本地备份与回归截图（不入库）
_bak_*/
_ux_shots/
_verify_shots/
beauty.worktree_*.bak
quiz.bak_r*.html

# kb-admin 真实 PDF 验证套件（含生成的测试 PDF，不入库；node_modules 勿提交）
__pdfqa/

# 离线大文件（站点不需要，避免撑爆仓库）
*.pptx
*.docx
*.pdf
*.xlsx

# ---- 纯构建产物：本地保留、不入库（2026-09-22 仓库瘦身）----
# 以下两者都可由仓库内脚本 100% 重生成，每次构建都会整份重写，体积合计约 100MB
#   在线版/                     <- _build_hosted.py 产出（瘦壳 + mods/*.gz + assets + 3D 副本）
#   客舱小助手（离线完整版）.html  <- _build_4in1.py 产出（全部模块 gzip-base64 内嵌的单文件版）
# 需要时重跑对应脚本即可；要临时查旧版可从历史取：git checkout <commit> -- <path>
在线版/
客舱小助手（离线完整版）.html

# 站点图集是「必需入库」的：模块 HTML 只保留 assets/img/* 相对引用（2026-09-22 由内联 base64 抽出）
!assets/
!assets/img/

# 手册解析临时文本与图片素材（qa.html 已内嵌知识库）
docs/manual_parse/
docs/manual_imgs/
docs/med_img/

# 数据包发布工作区（待发布 JSON 放这里，发布成功后产物在 packs/packs/ 入库）
packs/incoming/

# 一次性迁移工具归档（如需入库请移回根目录）
_legacy/

# AI 工具工作目录（设计/计划文档已归档至 docs/superpowers/）
.claude/

# ---- 本地开发/测试一次性产物（不入库，2026-09-20 增补）----
# 根级所有下划线开头文件/目录：调试脚本/截图/审计/日志产物（已跟踪的构建脚本不受影响）
/_*
APK封装/
PWA封装/
PakePlus-iOS-main/
qa5000_eval/
cbt_edge_profile/
cbt_截图样例/
ppt_images/
processed_image_*.png
*.bak_*.html
*.bak_2*
err.log
抢先版.ipa

# KB 源数据（知识库已内嵌 qa.html；大撤/CBT 原始素材与图片不入库）
大撤/
CBT练习题库/
/cbt_*.json
/cbt_*.md
/cbt_*.csv
/cbt_*.py
docs/dache_img/

# 一次性审计/测试报告文档（不入库）
*审计报告*
*审计明细*
*排查修复报告*
*核验报告*
*核对报告*
*改造报告*
*审查报告*
*完成报告*
*核查报告*
*美化报告*
*执行记录_*
*功能清单_*
*翻译测试记录_*
*归类手册解析_*
*多设备优化审计报告*
docs/日期匹配引擎说明.md