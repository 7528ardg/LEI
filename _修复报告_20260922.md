# 修复报告 · 2026-09-22（P0 + P1 + P2 全量）

> 上游：《系统性代码审查报告（2026-09-22）》共 **3 高危 / 11 中危 / 16 低危**
> 本次范围：**全部（P0+P1+P2）**；打包档位：**站点重建 + APK 重打**
> 结论：**全部处置完毕**，全量回归 **65/65 套通过（exit=0）**，APK/PWA 已重装配。

---

## 一、执行摘要

| 项 | 结果 |
|---|---|
| 处置发现 | 30 / 30（30 = 3 高 + 11 中 + 16 低） |
| 其中「审查报告之外、修复过程中实测新发现」的缺陷 | **4 个**（含 1 个实战级功能瘫痪） |
| 站点重建 | ✅ `_build_all.py build` exit=0（spring 7.99MB / 离线完整版 24.28MB / kb-admin 6.38MB） |
| APK 重打 | ✅ 36.03MB（37,777,842 B）／签名指纹未变／`_check_apk_sync` 13 份 HTML 一致／`_e2e_apk_www_test` **30/30** |
| PWA 重建 | ✅ 10/10 结构校验通过 |
| 全量回归 | ✅ 语法 OK + 逻辑 **0 套失败 / 共 65 套** |
| 新增守护 | `_verify_pwd_hash_20260922.js`（41 项，已挂入构建链） |

---

## 二、P0（立即修）· 3/3

### H1 · beauty.html 存储型 XSS（标签 / 分类 / 脚本 id 三处未转义）
- **改法**：三处插值全部走 `escHtml()`：`escHtml(tag)`、`escHtml(state.scriptLibFormContent)`、`escHtml(state.scriptLibFormTags.join(','))`、`escHtml(catKey)` + `escHtml(script.id)`（3 个 onclick 按钮）。
- **新增回归**：`_verify_scriptlib_xss_20260922.js`（真实浏览器）**21/21**——注入串不落地、CRUD 正常挂载、闸缺失时拒绝写入、正常保存可用。

### H2 · qa.html 聊天区 O(n²) 全量重写 + 无限增长
- **改法**：① `CHAT_MAX_MSGS = 200`，容量裁剪集中到一处；② 新增 `msgHtml(m)` / `stampMsgIds()`，`renderChat` 改为**纯增量 append**（`data-cc-rendered` + 首条 id 游标），不再整段重写；③ 清空 / 变更走同一条路径。
- **新增回归**：`_verify_qa_chat_render_20260922.js`（真实浏览器）**14/14**——不整段重写、上限 200、末条变更仍重绘、清空后重建、无页面级报错。

### H3 · `_sync_shell_js.py` 非原子写核心构建脚本
- **改法**：`write()` → `.tmp_write + os.replace`；补 `if __name__ == '__main__':` 守卫；删除 28 行死代码。
- **验证**：连跑两次 → 第二次 `[skip] 无需变更`（幂等）。

---

## 三、P1（本周内）· 5/5

### M1 · 管理员口令（3 位明文，值已在下文掩去）硬编码（源码 + 构建产物 + APK/PWA）
- **改法**：新增**口令哈希层**（纯 JS 同步 SHA-256，因 `file://` 下 `crypto.subtle` 不可用）＋ 每用户随机盐 ＋ **800 轮迭代**。
  - `_shell_enhance.js`（spring / 4in1 两模板的单一来源）与 `index.html` 各自植入同构实现；
  - `ADMIN` 改为 `{ 工号, 姓名, 手机号, 盐, 密码哈希 }`，**移除明文字段**；
  - 注册只写 `盐 + 密码哈希`；登录走 `verifyUserPwd`；旧明文记录**首次校验成功后原地升级**。
- **验证**：新增 `_verify_pwd_hash_20260922.js` **41/41**——明文清零（源码 + 4 份产物）、ADMIN 种子哈希与算法自洽、**两套实现摘要逐位一致（防双源漂移）**、行为 13 项、会话口令不落盘 5 项。

### M2 · 口令 / 安全问题答案明文落盘
- **shell（`_shell_enhance.js` + index.html）**：口令、安全答案均改为加盐哈希；旧记录命中即升级。
- **performance.html**：备份加密口令由 `sessionStorage` **明文持久化**改为**内存态 `_sessionPwd`**（刷新即失效，安全性优先于便利性）；`init()` 中一次性清除历史遗留的 sessionStorage 值；三个对外方法签名保持不变（6 处调用点零改动）。

### M5 · `weakBest` 死代码 + 重复检索
- **改法**：删除 `weakBest` 全段；新增 `crossPoolScan()` **记忆化跨库扫描**，被「无争议领先者判据 / 切库 / AI 上下文」三处复用，**消除 3 次重复扫描**（原每个走知识链的问题约 10+ 次线性扫描 3476 条）。

### M6 · `chatLLMOnce` 主 `res.json()` 未保护
- **改法**：`res.json()` 包 try，失败抛 `BAD_RESPONSE` 并纳入 `aiRetryable` → 走降级链而非直接抛错。

### M11 · 话术库入库闸兜底放行
- **改法**：保存 / 复制前置 **fail-closed**：`window.scriptLibGuard` 缺失 → `showAlert('合规校验未加载')` 并中止。
- **同步**：`_apply_scriptlib_guard_20260921.py` 的 NEW_SAVE / NEW_COPY 两道闸同步改为 fail-closed，`--check` 全在位。

---

## 四、P2（排期优化）· 8/8

### M7 · 澄清链竞态（`qaClarNone` 绕过串行化）
- **改法**：`qaClarNone` 的 `await process(orig)` 改走 `_qaAskChain`，杜绝 AI 生成中点「都不是」并发改写 `qaSource` 导致**答案错库**。

### M8 · beauty 话术库全量重渲（1045 条一次拼入 `innerHTML`）
- **改法**：① 搜索框 `oninput` 改 **180ms 防抖**（原先每敲一个字整页重绘）；② 列表改**渐进式渲染**：每屏 40 条，底部「显示更多（还有 N 条）」每次 +60；③ 过滤条件（栏目 / 搜索词 / 情绪）变化时自动重置回第一屏（`_slKey` 比对，无需改各按钮）。
- 首屏 DOM 量由「全部 1045 条」降为 **≤40 条**，搜索输入不再逐字重建整页。

### M9 · `csApplyImport` 按索引比对 → 误报「不一致」
- **改法**：改为**按 uid 比对**（uid 是条目的稳定身份，与位置无关）。「合并追加」保留包外条目属预期，仅「整包还原」才统计「工作区多出条目」。提示文案同步改为「按 uid 比对」。

### M10 + L13 · 静默丢弃无告警 / `verifyPack` 不验 version
- **引擎（单一来源 `docs/_fullpack_engine.js`，经 `_sync_fullpack.py` 注入）**：
  - 新增 `PROTOCOL_VERSION = 1`，`verifyPack` 校验 `version`（缺失 / 非法 / 高于 / 低于本引擎 → 四级告警并写入 `stats.version`）；
  - 逐源统计 `dupUid`（重复 uid）与 `noData`（缺 data），**升级为显式告警**，并进入 `stats`；
  - `applyFullPack` 报告新增 `droppedNoData` / `dupUid` / `dataLoss`；
  - `diffToEdits` 新增可选 `diag` 回填「被丢弃条目数 + 原因」。
- **kb-admin 界面**：导入预览、导入结果、导出结果三处均新增 **协议版本行 + 告警盒 + 数据丢失计数**；「整包还原」下 `dataLoss > 0` 即判为不一致。
- **验证**：`_verify_kb_fullpack.js` **51/51**、`_verify_kb_console_e2e.js` 全绿；`_sync_fullpack.py --check` 幂等 OK。

### M4 · `kbStrong` 口径
- **改法**：保留现有判据（当前打分数值下功能等效），**补注释说明隐含前提 `sem`**，避免后续改打分时踩坑。

### 低危 · 安全项
| 项 | 改法 |
|---|---|
| origin 校验 | `index.html` 新增 `ccMsgTrusted(e)`，2 处 `message` 监听（2356 / 4407）加来源校验；`qa.html` 新增 `ccMsgTrustedQa`；`_apply_qa_bridge_20260919.py` 的深跳监听新增 `_ccMsgTrustedQa`（同源 / `file:` / 自身 iframe 三选一） |
| `noopener` | `qa.html`×2、`quiz.html`、`risk-lite.html`、`_apply_pet.py`、`_apply_qa_bridge_20260919.py` 的 `window.open` 全补 |
| `app_base_url` 白名单 | `index.html` `getModBase()` 引入 `APP_BASE_ALLOW = ['', APP_MIRROR_BASE]`，**拒绝被篡改的镜像基址**（XSS 后无法重定向模块加载） |
| `postMessage` 目标收敛 | 原先一律 `'*'` → 现按上下文取值：qa→父窗口用新增 `ccParentOrigin()`（由 `document.referrer` 推导父页真实 origin，跨源镜像亦适用，取不到才回退 `'*'`）；壳层→iframe 用 `new URL(f.src).origin`。同步到 4 处发送侧与 3 份模板源 |
| `new Function` | `risk-lite.html` 的 `onclick` 迁移加**严格白名单**（仅「函数名(字面量参数)」）；不匹配者**保留原生 `onclick` 属性不迁移**——既杜绝任意代码执行，又不丢任何既有交互 |
| `window.eval` | `beauty.html` 删除 `window.eval('switchTab')` 代码异味，改用约定式 bare 引用 + `typeof` 兜底；并**补上 `window.switchTab` 导出**（此前深跳桥只能靠「找按钮再 click」兜底） |

### 低危 · 工程项
| 项 | 改法 |
|---|---|
| `newline=''` 缺失 | `_build_kbadmin.py` / `_build_hosted.py` / `_build_home.py` 全部补齐 |
| `_check_needles.py` 静默丢字节 | 解压后 `.decode('utf-8','ignore')` → 严格 UTF-8，失败计入 fail |
| `_normalize_lf` 双倍 IO | 单字节读缓存；并排除 `*_tmp*.js` / `*.tmp_write`（含 24MB 临时文件） |
| `_build_hosted.py` 非原子写 | 产物拷贝改 `.tmp_write + os.replace`（L12） |
| `_sync_fullpack.py` 回滚非原子 | 同上（M3） |
| `esc()` 死代码 / 工号正则不一致 | 删除；`^\d{4,8}$` → `^\d{4,10}$`（与 `maxlength=10` 对齐） |

---

## 五、修复过程中新发现并处置的 4 个缺陷（审查报告之外）

### ★ 1. beauty.html 话术库全部 CRUD 按钮 `ReferenceError`（实战级功能瘫痪）
- **现象**：真实浏览器探针实测 `window.saveScriptLib === undefined`（页面加载后）。
- **根因**：补丁把 `window.copyScriptById / editScriptLib / deleteScriptLib / saveScriptLib` 的 **EXPORTS 块嵌进了 `copyScriptById` 函数体内部**，且 `copyScriptById` 自身从未挂到 `window` → 四个入口一个都不存在，点按钮即 `ReferenceError`。
- **排查手段**：花括号配平不可靠（模板字符串干扰）→ 改用 AST（acorn）作出权威判定。
- **改法**：EXPORTS 迁到顶层（`const insertEmotionResponse` 之前，所有 `const` 声明之后）；`_apply_scriptlib_guard_20260921.py` 的锚点同步迁移。**21/21 回归验证 CRUD 已挂载。**

### ★ 2. 构建链「互踩」：单一来源同步 vs 图片外置，导致整链在第 5 步硬停
- **现象**：`_build_all.py build` 在第 5 步 `_sync_dache.py --check` 硬停：`quiz.html 与单一来源不一致`。
- **根因**：`_sync_dache.py`（构建第 5 步）把 `__DACHE_IMG_*__` 占位符替换为 **内联 data URI**；而 `_extract_inline_images_20260921.py`（第 20 步）又把这些大图抽成 `assets/img/*`。两者**互为逆操作** → 每轮构建必留漂移。此前只能「手工跑 `_sync_dache.py` 绕开」，漂移每轮复现（大撤 4 张长图共 2.07MB）。
- **改法**：让 `_sync_dache.py` **直接产出已外置形态**：`assets/img/<md5(base64)[:8]>.jpg`（与抽取器同口径，内容寻址、按需落盘、幂等）。现在 check 稳定通过、抽取器无事可做。
- **验证**：`_sync_dache.py --check` → `qa.html / quiz.html` 双 UP-TO-DATE；四张长图 md5 对应关系实测 `85893b92 / 8a07c3fe / c3640803 / 68d6307b` 全部命中。

### ★ 3. APK 装配自检判据过期 → 明明正常也报 `[FAIL]`
- **现象**：`_apply_apk_fix_20260918b.py`（v3）自检 `[FAIL] v2 预热收敛保留`。
- **根因**：v2 的自检判据早已随壳层「预取门控」改为 `('APK_PREWARM_V2' in out) or ('function _pwEnv' in out)`，**但 v3 的同名判据没跟着改**，仍只看 `APK_PREWARM_V2`。产物实际含 `_pwEnv`，功能正常。
- **改法**：v3 判据与 v2 对齐。复跑后 28 项自检全 OK。
- **风险提示**：该 FAIL 出现在装配链末端，若被忽略会掩盖真正的 A 阶段失败（A 阶段失败时 `www` 不更新、APK 字节数与旧包完全相同，极易误判为「打包成功」）。

### ★ 4. APK / PWA 内 `index.html` 仍带明文口令
- **改法**：由 APK 重装配 + PWA 重建自动修复（源头是已修复的 `_shell_enhance.js`）。现将 4 份产物纳入 `_verify_pwd_hash_20260922.js` 的**强断言**，再出现即失败。

---

## 六、验证证据

```
语法检查  16 份源/产物  SYNTAX_OK 全部通过
逻辑回归  65 套 / 0 套失败 / exit=0
关键套件  _verify_kb_fullpack.js        51/51     （引擎往返 + M10/L13）
          _verify_kb_console_e2e.js      全绿      （kb-admin 真实浏览器端到端）
          _verify_scriptlib_guard.js     34 项     （入库闸四道闸）
          _verify_scriptlib_xss_20260922.js  21/21  （新增·H1 XSS）
          _verify_qa_chat_render_20260922.js 14/14  （新增·H2 增量渲染）
          _verify_pwd_hash_20260922.js   41/41     （新增·M1/M2 守护）
          _verify_embed_gap_20260922.js  11/11     （嵌入态死带空白）
          _e2e_apk_www_test.js           30/30     （APK 真实载荷）
          _check_apk_sync.py             13 份一致
          _check_needles.py / _verify_nokey_all.py  通过（源码零内置密钥红线成立）
```

**分发版**

| 产物 | 结果 |
|---|---|
| `spring-assistant.html` | 7.99 MB，`ADMIN` 已哈希、0 处明文 |
| `客舱小助手（离线完整版）.html` | 24.28 MB，0 处明文；`assets/img` 已回填 data URI（单文件自包含断言通过） |
| `kb-admin.html` | 6.38 MB |
| `APK封装/CabinAssistant/客舱小助手.apk` | **37,777,842 B（36.03 MB）**；含 `assets/img` 127 / `ccmodels` 52 / glb 24 / 12 模块；签名 SHA-256 `23e8f485…23e9f9` 未变 |
| `PWA封装/客舱小助手/` | index 392,161 B；manifest / sw / 4 图标 10/10 通过 |

---

## 七、未处置项（附理由，需专项排期）

1. **L14 · `qa.html` `process()` 约 230 行超长函数 / 六库决策散落 5 处**：纯重构，行为面广、回归成本高；当前判据已由回归全覆盖，**建议专项做**。
2. **L16 · `var/let` 混用、`==` 宽松比较、魔法数字未集中**（熔断 60s、`s>=4`、`+15`）：量大且无功能影响，改动会在 diff 里淹没本次安全修复，**建议独立提交**。
3. **M8 的进阶形态 · beauty 事件委托化 / 虚拟滚动**：本次已用「防抖 + 渐进渲染」把首屏 DOM 从 1045 条降到 ≤40 条；再往下需重写列表渲染范式，收益递减而回归面大。
4. **L6 / L7 · 隐私告知文案**（联网搜索把查询词发第三方；用户自带 AI Key 明文存 `localStorage` 属设计内）：需产品口径确认，代码侧无可改。
5. **M1 的根因面 · 前端门禁本身可绕过**：本项目门禁定位为「本机 UX 门禁」而非安全边界。真正要防越权需服务端校验 —— 与 **AI Key 策略（推荐「丙·自建中转」）** 一并待决。
6. **仓库卫生（新发现）**：`/_*` 规则把 `_shell_enhance.js`、`_sync_dache.py`、`_apply_pet.py`、`_apply_qa_bridge_20260919.py` 等**构建链必需脚本**挡在 git 之外，新克隆无法完整重建。本次新增的 `_verify_pwd_hash_20260922.js` 已按既有惯例 `git add -f` 入库；其余历史脚本是否补入库，**建议单独决策**（会影响仓库体积与公开面）。

---

*报告生成于 2026-09-22 · 全部结论均以「构建链实跑 + 真实浏览器回归 + APK 真实载荷实测」为依据，未采用静态推断代替验证。*
