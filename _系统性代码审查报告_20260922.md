# 系统性代码审查报告（融合版）

- **审查日期**：2026-09-22
- **审查范围**：生产代码约 24 万行 —— 12 个站点模块（index / qa / beauty / kb-admin / cc-home / daily / performance / quiz / manual / medical / risk-lite / spring-assistant）、3 个 JS 引擎（docs/_fullpack_engine.js、_talkshow_engine_v2.js、_shell_enhance.js）、12 个 Python 构建/维护脚本
- **排除**：`_bak_*` 备份、`*_tmp*` 临时文件、一次性审计脚本、`__pycache__`
- **方法**：4 路专项并行审查（安全 / Python 构建链 / 前端模块 A / 前端模块 B）+ 主审查人对全部高危及关键中危发现逐条回源验证（行号与代码上下文均已核实）
- **维度**：逻辑正确性与边界、安全性、性能与资源、异常处理、可读性与可维护性、编码规范

---

## 一、总体评价

项目整体工程质量**中上**：安全红线（源码零内置 AI 密钥）成立，AI 渲染主链路有转义保护，Python 构建链无 `shell=True`/`rmtree` 等危险调用，幂等注入机制健全，绩效敏感数据有 AES-GCM 加密。**但存在 1 个确凿的存储型 XSS（可被跨用户触发）、凭据管理整体薄弱（管理员口令硬编码 + 用户口令明文落盘）、构建维护脚本两处非原子写可截断关键文件、qa 聊天渲染长会话 O(n²) 内存泄漏**。

- 高危 3 项 ｜ 中危 11 项 ｜ 低危 16 项（合并后）
- 最优先：修复 beauty.html 存储型 XSS（P0）

---

## 二、高危问题（3 项）

### H1. beauty.html 存储型 XSS —— 话术标签与表单回填未转义
- **位置**：`beauty.html:29773`（`${tag}` 直接拼接）、`29816`（textarea 回填 `${state.scriptLibFormContent}`）、`29819`（value 属性回填 `${state.scriptLibFormTags.join(',')}`）
- **影响**：tags 由用户在新增表单录入（31888），且脚本库可经 kb-admin 导入，属**存储型 XSS、可跨用户触发**。构造 `</textarea><img src=x onerror=...>` 或含 `"` 的属性逃逸即可执行任意脚本，进而读取 localStorage 中的用户口令、AI Key 等全部数据
- **触发条件**：录入/导入含恶意标签的话术 → 打开话术库列表或编辑表单即触发
- **佐证**：话术正文渲染经 `slRender→esc`（879）是安全的，问题恰在"正文以外"的三个拼接点
- **修复**：三处统一走 `esc()/escHtml()`；textarea 回填改用 `textContent` 赋值；新增回归断言（可挂 `_verify_scriptlib_guard.js`）

### H2. qa.html 聊天渲染全量重建 + 消息数组无上限 —— 内存泄漏与 O(n²) 卡顿
- **位置**：`qa.html:7167`（`wrap.innerHTML = chat.msgs.map(...)`）、`6799` / `9988` / `10020`（msgs 只增不减，无容量上限）
- **影响**：长会话下 ① `chat.msgs` 无限增长（含大段 HTML/天气卡）→ 内存持续泄漏；② 每条新消息重建全部气泡并重跑 `qaWrapBetterAnswer/qaFbBar`（7170-7171）→ 单条 O(n)、整体 O(n²)，数百条消息后明显掉帧甚至页面卡死
- **触发条件**：单会话累计数百条问答（培训/练习场景极易达到）
- **修复**：`chat.msgs` 设上限滚动裁剪（如保留最近 200 条）；`renderChat` 改增量 append 新节点而非全量重写

### H3. _sync_shell_js.py 非原子写关键构建脚本 —— 中断可截断 _gzip_build.py / _build_4in1.py
- **位置**：`_sync_shell_js.py:27-30`（`write()` 直接 `io.open(p,'w').write(s)` 覆盖写）；且模块顶层执行、无 `__main__` 守卫
- **影响**：违背项目"落盘原子写"铁律。Ctrl-C / 进程被 kill / 磁盘满时，两个核心构建脚本被截成半截，整条构建链崩溃且不易察觉（下次跑链才爆）
- **触发条件**：同步执行期间中断
- **修复**：改 `.tmp_write + os.replace`；补 `if __name__=='__main__':` 守卫；删除 28 行死代码 `s.encode('utf-8')`

---

## 三、中危问题（11 项）

| # | 位置 | 问题 | 影响与触发条件 | 修复建议 |
|---|------|------|----------------|----------|
| M1 | `index.html:2604`、`spring-assistant.html:1850`、`_shell_enhance.js:12` | 管理员口令（3 位明文，值已在下文掩去）硬编码 | 任何取得静态文件者可管理员登录、重置他人密码；门禁纯前端本身可绕过（UX 级） | 口令移出源码，首次运行强制设置并加盐哈希存储；文档明确"本机 UX 门禁"定位 |
| M2 | `index.html:2817/2825`（localStorage `cabin_users_v1`）、`_shell_enhance.js:19-28`、`performance.html:13136`（sessionStorage 明文口令） | 用户口令、安全问题答案**明文落盘** | 同机他人或任一 XSS 可读取全部账户口令 | 加盐哈希（SHA-256+salt 或 PBKDF2）后存储；sessionStorage 口令改内存态 |
| M3 | `_sync_fullpack.py:126` | 回滚写 `kb-admin.template.html` 非原子 | 中断即损坏模板 | 同 H3，tmp+os.replace |
| M4 | `qa.html:7992` | `kbStrong` 多了未文档化的 `sem` 门槛，与约定 `s>=4 && (s>=8||sim>=0.25||ev>=2)` 不一致 | 当前打分下功能等效，但后续改打分极易踩坑 | 对齐约定或补注释说明隐含前提 |
| M5 | `qa.html:8433-8444` + `8498-8505` | `weakBest` 计算后**从未被消费**（死代码，已全文件验证仅赋值无读取）；且每个走知识链的问题在 8434 与 8498 两段各对 5 库做 `kbSearch`，约 10+ 次线性扫描 3476 条 | CPU 浪费，连续快问掉帧 | 删除 weakBest 死代码；AI 上下文复用第一段检索结果 |
| M6 | `qa.html:2628` | `chatLLMOnce` 主 `res.json()` 未 try | 代理返回 HTML 错误页时抛 SyntaxError，且不计入 `aiRetryable` → 不降级直接抛错 | try 包裹并视为可重试错误走降级链 |
| M7 | `qa.html:8164` vs `7307` | `qaClarNone` 内 `await process(orig)` 绕过 `_qaAskChain` 串行化 | AI 生成中点"都不是"会并发改写 `qaSource`，答案错库 | 改走 `_qaAskChain` |
| M8 | `beauty.html:30662` + `29764` | `render()` 整页 `innerHTML` 重建；1045 条话术一次全量拼入、内联 onclick 无事件委托 | 搜索/翻页即全量重渲，话术库继续增长后卡顿 | 列表分页或虚拟滚动；改事件委托局部更新 |
| M9 | `kb-admin.html:96104-96119` | `csApplyImport` 按**索引**逐条比对工作区与包内容 | replace 模式不还原顺序时误报"不一致"（false positive），误导运维 | 按 uid 比对 |
| M10 | `kb-admin.html`（applyFullPack / diffToEdits）、`docs/_fullpack_engine.js` | 包内**重复 uid** 或条目**缺 data** 时静默丢弃、无告警 | 数据丢失不可见，4914 条全量包场景难排查 | 导入报告增加"丢弃条目数+原因"明示 |
| M11 | `beauty.html:31847` | 入库闸兜底放行：`window.scriptLibGuard` 缺失时 `ok:true` 静默通过（已修正此前"入库不调闸"的误判——闸实际会被调用，但存在此软缺口） | 闸脚本未加载/被绕过版本漂移时，违禁词话术直接落库 | 闸缺失时 fail-closed（拒绝入库并提示），或在保存前断言 `scriptLibGuard` 存在 |

---

## 四、低危问题（16 项，合并简述）

**安全类**
1. `index.html:2356`、`qa.html:10275/10374` message 监听未校验 `event.origin`；`qa.html:2771/10299/13631` postMessage 目标 `'*'` —— 当前仅触发 UI 切换，跨源嵌入场景需补白名单
2. `qa.html:2776/10437/13635`、`quiz.html:10981`、`risk-lite.html:17958` `window.open` 未带 `noopener`
3. `risk-lite.html:21384` `new Function('event', code)` 转换 onclick 属性 —— 当前 code 来自开发者属性，若未来 innerHTML 注入外部 onclick 即升级为代码执行
4. `beauty.html:33111` `window.eval('switchTab')` 常量求值，代码异味
5. `index.html:1814` `app_base_url` 可由 localStorage 覆盖为任意镜像基址 —— XSS 后可重定向模块加载，建议白名单校验
6. `beauty.html:32614/32637` 联网搜索把查询词发第三方（tavily/firecrawl），建议隐私告知
7. `qa.html:2533/2719` 用户自带 AI Key 明文存 localStorage（设计内），建议界面提示风险

**Python 构建链**
8. `_check_needles.py:165` 解压后 `.decode('utf-8','ignore')` 与本文件 `rd()`"禁止 ignore"口径自相矛盾，可能静默丢字节漏检
9. `_build_kbadmin.py:62-63/79/86/87`、`_build_hosted.py:172/184`、`_build_home.py:2386` 读文本缺 `newline=''`（靠 `_normalize_lf` 兜底，属隐患）
10. `_sync_shell_js.py:48-68` `grab_fn` 花括号配平不跳过字符串/正则/模板字面量，函数体含 `"{"` 时会误切
11. `_normalize_lf_20260921.py:60-91` 每文件 scan/normalize 双读，且未排除 24MB 临时 js，双倍 IO
12. `_build_hosted.py:139` `shutil.copyfile` 等非原子产物写（重跑可覆盖，危害低）

**前端逻辑/规范**
13. `docs/_fullpack_engine.js:631` `verifyPack` 只验 magic/kind 未验 version；引擎与 kb-admin 内嵌副本（447-944）双源漂移风险
14. `qa.html:7979` 同分裁决用 sem 布尔而非 ev 计数，极端边界仍可能选错条目；`8312-8541` `process` 函数约 230 行超长，六库决策散落 5 处
15. `_shell_enhance.js:119` `esc()` 死代码；注册工号正则 `^\d{4,8}$` 与 input `maxlength=10` 不一致
16. 跨文件：`var/let` 混用、`==` 宽松比较（如 qa.html:2146）、魔法数字未集中（熔断 60s、s>=4、+15）

---

## 五、各维度"未发现问题"清单（负向结论，同样重要）

- **硬编码 AI 密钥**：未发现。`_3d_token.txt` 未被任何生产代码引用/打入产物；`__QA_BUILTIN_AI__` 在构建链中不被赋值，"源码零内置密钥"红线成立
- **AI 回答渲染 XSS**：qa `mdToHtml`（8664）先 `esc()` 再生成 HTML、用户问句 `addMsg(esc(q))`，安全；beauty 竞品渲染 `escHtml`（32207）已有修复记录
- **动态代码执行**：无 `eval(用户输入)` / `setTimeout(字符串)`
- **Python 危险调用**：无 `shell=True` / `os.system` / `shutil.rmtree` / `eval`；subprocess 均列表形式
- **幂等注入**：未发现 strip 不净 / 重复注入 / 漏匹配；各大标记块（CC_AI_COMPANION_v1 等）幂等守卫齐全
- **嵌入态/首屏约定**：max-height `!important`、MutationObserver 防重绑、`QA_SHOW_WELCOME=false`、常问条不自动显示，均符合
- **正则注入**：用户输入未进入 RegExp 构造器；唯一 `new RegExp`（beauty 31103）来自静态关键词表
- **绩效数据安全**：AES-GCM 加密落 IndexedDB，localStorage 仅存密文，密钥管理得当（正向实践）

---

## 六、优先修复清单

**P0（立即修）**
1. H1 beauty.html 三处未转义拼接（29773 / 29816 / 29819）→ 走 esc
2. H3 + M3 `_sync_shell_js.py` / `_sync_fullpack.py` 原子写改造 + `__main__` 守卫

**P1（本周内）**
3. H2 qa renderChat 增量渲染 + msgs 容量上限
4. M1/M2 凭据治理：管理员口令移出源码、用户口令哈希化
5. M5 删除 weakBest 死代码、复用检索结果
6. M6 chatLLMOnce JSON 解析保护
7. M11 入库闸 fail-closed

**P2（排期优化）**
8. M7 澄清链竞态、M8 beauty 渲染性能、M9/M10 kb-admin 导入健壮性（uid 比对/丢弃告警/version 校验）
9. M4 kbStrong 口径对齐；低危安全项（origin 校验 / noopener / app_base_url 白名单）
10. 低危工程项（newline='' 补齐、_normalize_lf 双读、死代码清理、常量集中）

---

*审查方式说明：本报告由 4 路并行专项审查产出初稿，全部高危及关键中危发现已经主审查人回源逐条验证（含一处代理误判修正：M11 入库闸实际会被调用，问题修正为"闸缺失静默放行"）。*
