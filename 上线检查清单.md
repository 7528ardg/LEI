# 客舱小助手 · 上线前最终检查清单

> 版本：2026-08-28（Agent 改造版，含 话术合规审查 / 错题薄弱点分析 / 总调度路由）
> 部署目标：GitHub Pages → <https://7528ardg.github.io/LEI/（仓库> 7528ardg/LEI，has\_pages=true）
> 使用方式：逐项核对，每项完成后在 `[ ]` 中打 `[x]`；命令均在工作目录 `c:\Users\Admin\Desktop\融合版` 执行。

***

## A. 数据一致性（知识库）

* [ ] **A1. qa.html 与 docs 源文件同步**：`_kb_ccm_new.js`（CCM 256）/ `_kb_mgm_new.js`（MGM 135）/ `_kb_svc_new.js`（SVC 66）与 qa.html 内嵌 `window.KB_*_RAW` 完全一致
  ```powershell
  python _verify_qa_merge.js   # 期望：20 passed, 0 failed（已归档 _legacy/，由 _build_all.py verify 一键回归覆盖）
  ```

* [ ] **A2. kb-admin 产物同步**：kb-admin.html 从 qa.html 提取四库 + 内联 pdf.js/cmaps 重建后仍自包含
  ```powershell
  python _build_kbadmin.py
  python _verify_kbadmin.js    # 期望：全部 PASS，kb-admin.html 无 __BASE_* 占位符残留（已归档 _legacy/，构建链已含语法检查）
  ```

## B. 构建产物

* [ ] **B1. 重建单文件版 spring-assistant.html（9 模块 gzip 内嵌）**
  ```powershell
  python _gzip_build.py
  ```

* [ ] **B2. 重建离线完整版 客舱小助手（离线完整版）.html（10 模块含 risk）**
  ```powershell
  python _build_4in1.py
  ```

* [ ] **B3. 外壳置换校验**
  ```powershell
  node _verify_build.js        # 期望：模块 qa,quiz,performance,beauty,medical,daily,manual,report,kbadmin + pako/DecompressionStream/fallback 全 true（已归档 _legacy/）
  node _verify_pako.js         # pako 注入校验（已归档 _legacy/）
  node _verify_fallback.js     # 降级路径校验（已归档 _legacy/）
  ```

* [ ] **B4. 三外壳页签与嵌入 CSS 一致**：index.html / spring-assistant.html / 4合1 均含 10 个页签（含 📇 库管理）、EMBED\_CSS 含 kb-top、risk
  ```powershell
  node _verify_qa_merge.js     # 前半段三外壳一致性（页签/kbadmin 映射/kb-top/wrap）期望全 PASS（已归档 _legacy/）
  ```

* [ ] **B5. 内嵌新 Agent 代码命中**：两单文件 zip 解压后 qa/quiz/beauty 含 detectJumpIntent、QuizSpringAI、COMPLIANCE\_RULES、runComplianceAudit
  ```powershell
  # 如无独立验证脚本，用 node 临时解压核验特征串；或直接跑 B3 后人工抽查
  ```

## C. 语法与静态校验

* [ ] **C1. 四个核心文件 JS 语法**
  ```powershell
  python _check_js.py          # 期望 beauty/quiz/performance/spring-assistant 均 OK（无 SYNTAX_ERR）
  ```

* [ ] **C2. 新增模块脚本语法**（qa.html、kb-admin.html 单独抽查）
  ```powershell
  node --check <提取后的 js>   # 或复用 _check_js.py 模式
  ```

## D. 功能回归（真机/浏览器）

* [ ] **D1. 三个新 Agent 功能**：
  * 话术合规审查：AI 对话练习输入区 ⚖️ 按钮 → 规则命中（比专柜/全网最低/手慢无等 10 词）+ AI 改写（已配 Key 时）

  * 错题薄弱点分析：错题本页 🤖 薄弱点分析 → 章节条形图 + 高频标签 + 反复错榜（无 Key 时规则版可用）

  * 总调度路由：你问我答输入"去绩效管理 / 打开医疗急救 / 进入模拟考试" → 自动切模块；"复飞怎么操作"不误跳

* [ ] **D2. 原有功能回归**：知识库检索（氧气瓶怎么用→CCM）、话术生成、搭配审查、题型训练、全局备份/恢复、主题切换、spring-switch 跨模块跳转

* [ ] **D3. 登录/权限**：performance（028981/LWH）、risk 模块登录后数据正常；退出后无越权

## E. 离线能力

* [ ] **E1. 完全断网（飞行模式/开发者工具 offline）**：
  * 4合1 离线版整页加载、gzip 解压正常

  * qa/quiz/beauty 模块切换正常，新增 Agent 规则路径可用、AI 请求优雅失败不白屏

  * 图片/字体无外链、无 404 控制台错误

* [ ] **E2. localStorage 持久化**：错题/绩效/美妆收藏/AI 配置断网重启后保留

## F. 合规与安全

* [x] **F1. 话术合规**：全库 10 项硬违禁词（比专柜/比代购/比免税店/全网最低/最低价/最便宜/手慢无/爆款/明星产品/错过就没有）已清零；beauty 饥饿营销变体（错过/限时/抢完/卖完/赶紧下单/不等人/机会难得/限量+别错过 等 90+ 句）已全部改写为陈述性话术（保留 划算/优惠/含税/一价全包）；客户台词（模拟旅客质疑）按规定保留；规则定义、库存标签、情感词表不动。

* [x] **F2. 密钥安全**：grep 扫描无硬编码 apiKey/sk- 明文；Key 仅存 localStorage(spring\_ai\_cfg)；两产物解压复查无泄漏。

* [x] **F3. 仓库卫生**：.gitignore 覆盖 pptx/docx/pdf/xlsx、docs/manual\_parse、.tmp\_\*、\_\_pdfqa；git status 无 node\_modules/大临时文件；.tmp\_mods\_beauty.html 已被 git check-ignore 确认忽略。

## G. Git 提交与发布

* [x] **G1. 提交前确认改动范围**：已核对，33 个文件（10 修改 + 15 新增构建/校验/模板 + libs 依赖 + 清单文档），不含 .claude/ 与一次性转换脚本；无 node\_modules/超大临时文件
  ```powershell
  git status --short    # 确认无 node_modules、无 __pdfqa、无超大临时文件
  ```

* [x] **G2. 暂存并提交**：已按清单以具体文件名暂存（非 git add .）→ 提交 `78ec74a`「feat: 上线前 Agent 改造与合规清洗（话术合规审查/错题诊断/总调度路由 + 违禁词清零）」；提交后仅剩本地工具未跟踪（符合预期）

* [x] **G3. 推送并确认**：线上已推送（78ec74a → acffef9）；GitHub 提示仓库已迁移 LEI，已执行 `git remote set-url origin https://github.com/7528ardg/LEI.git` 对齐 remote，后续 push 无迁移提示
  > 若远端仓库已更名，用：
  >
  > ```powershell
  > git remote set-url origin https://github.com/7528ardg/LEI.git
  > git push -u origin main
  > ```

## H. 发布后线上验证

* [x] **H1. 页面可访问（HTTP 200）**：`https://7528ardg.github.io/LEI/` → 200

* [x] **H2. 关键资源 200**：index/qa/quiz/beauty/spring-assistant/docs/\_kb\_ccm\_new\.js 均 200；kb-admin.html 200（GET 3.17MB，HEAD 偶发超时属网络波动）

* [x] **H3. 新代码特征串已在线上**：qa 含 detectJumpIntent + "渠道和清关都有保障"；beauty 含 COMPLIANCE\_RULES/runComplianceAudit 且 "别错过" 清零（线上仅剩规则定义 "bad: '爆款'" 属预期）；quiz 含 aiWrongDiagnosis

* [ ] **H4. 浏览器强刷验证**（线上新代码覆盖缓存）：Ctrl+F5 / 清除站点数据后打开 <https://7528ardg.github.io/LEI/> 确认四库检索与三个新 Agent 在线可用

***

## 快速失败红线（任一不满足不得上线）

1. `_check_js.py` 出现 SYNTAX\_ERR ↔ C1 失败
2. 四库数量不一致（CCM 256 / MGM 135 / SVC 66 / Daily 32）↔ A1 失败
3. 断网后 4合1 无法打开或模块白屏 ↔ E1 失败
4. 线上 qa.html 搜不到 detectJumpIntent ↔ H3 失败
5. 仓库出现 node\_modules / 明文 API Key / 大体积 pptx/pdf ↔ F3 失败

