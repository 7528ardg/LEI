# 春秋航空 · 广州分队客舱小助手 —— 产品使用清单与操作方法

> 适用对象：本机文件（`融合版` 目录）打包发布后的一线乘务员、班组长、管理员。
> 更新日期：2026-08-29

***

## 一、产品是什么

面向春秋航空广州分队的一线工作融合平台，把 **知识问答、培训考核、绩效管理、美妆话术、医疗急救、风险预警、日常问题、手册奖惩、事件报告、手册库管理** 十个工具整合为一个 App。

* 全部数据（错题、收藏、绩效、话术、备份等）都存在**本机浏览器**里，无需联网注册，换手机可用「备份/恢复」迁移。

* 内容（知识库 / 美妆产品 / 题库）以 **JSON 数据包**方式更新，团队发布新版后顶部会出现「📦 新数据包」提示，一键安装即自动生效，**不需要重装 App**。

***

## 二、产品清单（文件与入口）

| 文件                      | 用途            | 说明                                          |
| ----------------------- | ------------- | ------------------------------------------- |
| `index.html`            | **线上主入口（推荐）** | 快速拆分版外壳，约 18KB，十大模块按需加载，点击才下载对应模块，之后缓存不重复请求 |
| `客舱小助手（离线完整版）.html`     | **离线单文件版**    | 约 6MB，十大模块全部内嵌，双击即用，完全无网络依赖，适合离线环境          |
| `spring-assistant.html` | 离线单文件版（九模块）   | 约 5.3MB，不含风险预警模块                            |
| `qa.html`               | 💬 你问我答       | 四库知识问答 + AI 自由问答                            |
| `quiz.html`             | 📚 培训考核       | 刷题系统（练习/背题/模拟考试/错题本/成就）                     |
| `performance.html`      | 📊 绩效管理       | 绩效汇总、飞行小时、销售、档位追踪等                          |
| `beauty.html`           | 💄 美妆话术       | 产品库、智能话术匹配、话术生成                             |
| `medical.html`          | 🚑 医疗急救       | 机上急救处置流程、病症、药品设备、操作图鉴                       |
| `risk-lite.html`        | ⚠️ 风险预警（精简版）  | 风险地图、指标监控、数据分析                              |
| `daily.html`            | ❓ 日常问题        | 高频业务问题速查                                    |
| `manual.html`           | 📕 手册奖惩       | 手册要点、奖惩条款速查                                 |
| `report.html`           | 🗂 事件报告       | 事件报告流程速查                                    |
| `kb-admin.html`         | 📇 库管理（管理员）   | 手册 PDF 上传解析、覆盖率地图、差异审核、数据包中心                |
| `packs/manifest.json`   | 数据包发布清单       | 在线更新时程序自动读取比对                               |

> 手机使用建议：将 `index.html`（或离线完整版）添加到主屏幕（iOS：Safari「添加到主屏幕」；Android：浏览器菜单「添加到主屏幕」），即可像原生 App 一样全屏使用。

***

## 三、三种使用方式

### 方式 1：线上 / 拆分版（推荐日常使用）

1. 打开 `index.html`（团队部署后打开团队网址即可）。
2. 顶部出现十个页签，**点击对应模块才下载**，之后切换秒开、不再重复请求。
3. 弱网保护：某模块 15 秒加载超时会自动在主源与镜像线路间切换；双线路都失败时，点击加载页即可重试。

### 方式 2：离线单文件版（推荐无网环境）

1. 把 `客舱小助手（离线完整版）.html` 发给同事。
2. **双击文件**即用，无需安装、无需联网；手动切换到离线单文件时性能最优。

### 方式 3：数据包内容更新（不需要重新发文件）

* 团队发布新知识 / 新美妆产品 / 新题库时，App 启动会**自动检查更新**，顶部出现 `📦 新数据包(n)` → 点击 → 「⬇️ 全部安装」→ 即刻生效。

* 也可以由管理员在「库管理 → 数据包中心」手动导入/导出数据包 JSON。

```text
顶栏各按钮含义：
📦 新数据包(n) → 在线数据包更新（有团队新内容时出现）
💾 备份         → 数据备份 / 从文件恢复
☀️/🌙 主题按钮  → 切换浅色 / 深色模式
👤 头像/姓名     → 点击编辑个人资料
在线/离线状态点  → 网络状态指示
```

***

## 四、通用操作（所有模块通用）

### 1. 个人资料（姓名 / 头像）

* 点击顶栏头像 → 编辑姓名（≤12 字）、上传或删除头像 → 「💾 保存」。

* 资料保存在本机，随「备份/恢复」迁移。

### 2. 数据备份与恢复（换手机 / 防丢失）

* 点击顶栏 `💾 备份` → 「⬇️ 导出备份文件」→ 得到 `客舱小助手备份_日期.json`。

* 换机流程：把该文件通过微信「文件传输助手」/ 网盘发到自己 → 在新设备打开助手 → 备份 → 「⬆️ 从文件恢复备份」。

* 包含：错题本与成绩、绩效数据、美妆收藏与定制话术、AI 配置、手册收藏、风险预警设置等全部本地数据。

* **注意**：内容数据包（知识 / 销售 / 题库）属于团队发布内容，不随个人备份迁移（换机后重新点「📦 新数据包」安装即可）。

### 3. 主题（深色 / 浅色）

* 点击顶栏 ☀️ / 🌙 一键切换，偏好本地保存，所有模块同步生效。

### 4. 模块间跳转

* 顶部页签直接切换；问答等模块内也有「跳转到其他板块」的快捷入口。

### 5. 页面更新

* 版本更新后若界面还是旧的，请**强制刷新**：Ctrl + F5（电脑）/ 下拉重载（手机）。

***

## 五、十大模块操作详解

### 1️⃣ 💬 你问我答（qa.html）——日常查知识与 AI 辅助

**功能**：日常问题 / 客舱乘务员手册 01.10 / 客舱服务部管理手册 01.14 / 客舱服务规范 00.07 **四库**问答 + AI 自由问答 + 话术向导。

**操作方法**：

1. 点击顶部「💬 你问我答」页签。
2. 底部点击切换知识库：`日常库 / 乘务员手册 / 管理手册 / 服务规范`。
3. 输入问题，回车（Enter）发送；Shift + Enter 换行。
4. 示例问题：“病假怎么请”“帮我写一段销售话术”。
5. （可选）点击 AI 配置入口，按教程填入自己的 AI Key（仅存本机，可随时清除）→ 解锁「AI 自由问答」满血版。未配置时自动使用内置四库与规则生成。
6. 回复风格可用「四种风格」切换（存本机）。

**技巧**：回答以手册原文为准的条目会给出出处（章节号），可点击进入对应章节正文。

### 2️⃣ 📚 培训考核（quiz.html）——刷题系统

**功能**：题库（默认内嵌 + 支持上传自定义题库）、顺序练习、背题模式、章节专项、错题重练、模拟考试（自组卷）、错题本、收藏、历史记录、成就系统。

**导航（左侧栏）**：

| 菜单         | 用途                                 |
| ---------- | ---------------------------------- |
| 🏠 客舱（首页）  | 每日训练目标、快速开始、继续上次训练、题库上传            |
| ✈️ 答题·启航   | 进入练习答题（分模式）                        |
| 📜 模拟考试    | 一键配置预设模板 → 自组卷 → 实时得分 → 成绩统计       |
| 📋 题库·航班清单 | 查看/上传我的题库                          |
| 🗂 章节专项    | 按章节 + 难度专项练习                       |
| 📊 飞行数据    | 练习统计                               |
| ⬛ 错题本·黑匣子  | 错题列表 / 错题重练，掌握后自动标注                |
| ⭐ 收藏·VIP座位 | 收藏的题目（答题页 ⭐ 一键收藏）                  |
| 🕐 历史航段    | 历史训练记录                             |
| 🏅 成就·乘务成就 | 成就系统（解锁带庆祝弹层，含稀有度体系）               |
| ⚙️ 客舱设置    | 设计风格（A 经典航空 / B 现代极简 / C 商务高端）、音效等 |

**操作方法**：

1. 进入「培训考核」→ 首页选择模式（顺序练习 / 背题 / 错题重练 / 模拟考试）。
2. 答题：点击选项 → 立刻对错反馈 → 下一题。可 ⭐ 收藏、📝 添加笔记。
3. 模拟考试：进入「模拟考试」→ 选预设模板（或自组卷）→ 下一步 →「🚀 开始考试」→ 交卷后查看得分与成绩单，可保存成绩。
4. 未完成的训练**自动保存**，回到首页点「继续训练」即可恢复（关闭页面也不丢）。
5. 想用自定义题库：首页上传区选择 JSON 题库文件即可（上传后有格式校验）。

### 3️⃣ 📊 绩效管理（performance.html）——绩效看板

**功能**：汇总 / 绩效 / 飞行小时 / 销售 / 个人分析 / 班组排名 / 班组管理 / 档位追踪（含模拟预测警示）/ 综合分析驾驶舱 / 决策辅助（智能预警 · 个人画像 · 跨期对比）/ 回收站 / 操作日志 / 备份恢复 / 规则。支持从 Excel 导入数据。

**权限说明**：

* 默认进入「游客模式」，可浏览部分汇总数据。

* 点击「🔐 管理员登录」输入管理员账号密码后可查看/操作全部功能（导入、删除、班组管理、备份恢复等）。

* 管理员可在「客舱设置 / 账号管理」修改密码，并创建乘务员账号。

**操作方法**：

1. **导入数据**：进入「汇总 / 绩效」→ 点「📥 导入 Excel」→ 选择文件。支持自动识别工作表：乘务长、乘务员、销售、运行、综合、工作评定、安全质量、飞行小时。首次使用建议先下载导入模板，确保格式正确。
2. **查看绩效**：左侧导航选择「汇总 / 绩效 / 飞行小时 / 销售 / 个人分析」查看排行与图表。
3. **班组排名**：切换「班组排名」「班组管理」查看/维护班组。
4. **档位追踪**：查看每人档位变化趋势，含档位模拟预测与警示（持续低于平均会标红）。
5. **综合分析**（管理员）：综合分析驾驶舱，航班组对比、飞行小时趋势、时间对比等动态看板。
6. **决策辅助**（管理员）：智能预警（识别长期低绩效/低小时人员）、个人画像报告、跨期对比。
7. **备份恢复**（管理员）：模块内历史数据一键备份 / 恢复（另有顶栏全局备份）。
8. 删除误操作可到「回收站」找回；所有关键操作记录在「操作日志」。

### 4️⃣ 💄 美妆话术（beauty.html）——机上销售辅助

**功能**：产品库浏览、需求推荐（智能话术匹配）、肤质筛选、同品比拼、话术生成与衔接审查、已选产品、自定义话术、话术库管理、我的收藏、跨境指南、历史记录。

**页签（顶部）**：

| 页签       | 用途                               |
| -------- | -------------------------------- |
| 📦 产品浏览  | 全部产品（品牌筛选 / 排序：用户最多）             |
| 🎯 需求推荐  | 选择旅客需求（品类·效果·品牌·数量）→ 智能匹配产品 + 话术 |
| 🧴 肤质筛选  | 按肤质（干皮/油皮/敏感肌等）推荐                |
| ⚔️ 同品比拼  | 同类产品并排对比成分、卖点                    |
| ✨ 话术生成   | 一键生成销售话术（含衔接审查）                  |
| 🛒 已选产品  | 本次推荐选中的产品清单                      |
| 📝 自定义话术 | 自己维护的定制话术                        |
| 📚 话术库管理 | 管理话术库（支持数据包更新）                   |
| ⭐ 我的收藏   | 收藏的产品 / 话术                       |
| 📖 跨境指南  | 跨境销售合规要点                         |
| 📋 历史记录  | 最近生成 / 使用的话术                     |

**操作方法**：

1. 进入「美妆话术」→ 用「🎯 需求推荐」：勾选旅客肤质/想改善的问题/价位 → 系统匹配产品组合 → 点「✨ 生成话术」→ 得到可直接开口的销售话术（支持整段复制）。
2. 想找单品：用「📦 产品浏览」按品牌 / 类别筛选，或「🧴 肤质筛选」输入肤质推荐。
3. 不确定买哪个：「⚔️ 同品比拼」把两款产品放一起对比。
4. 常用的话术点 ⭐ 收藏，下次在「我的收藏」直接用。
5. 团队上线新美妆产品包后，顶部「📦 新数据包」出现即自动更新产品库与话术库。

### 5️⃣ 🚑 医疗急救（medical.html）——机上急救处置参考

**功能**：机上急救处置流程 / 常见病症处置 / 药品与设备 / 操作图鉴。

**操作方法**：

1. 选择「机上医疗事件」场景（或直接按病症搜索，如：心梗、气道梗阻、分娩、过敏、哮喘）。
2. 查看分颜色的处置轻重分级（🔴 红色威胁生命 → …），按步骤执行：呼救 → 通知乘务长 → 广播找医生 → 取设备 → 实施急救 → 报告机长（必要时备降/通知地面）→ 填写报告单。
3. 「药品与设备」页签可查机型（A320/A321）实际配备：氧气瓶、急救箱、应急医疗箱、医疗物品补充箱、卫生防疫包的数量与清单。
4. 操作图鉴：关键急救操作（CPR、气道管理等）图文步骤。

### 6️⃣ ⚠️ 风险预警 · 精简版（risk-lite.html）——风险管理（需登录）

**功能**：风险地图、指标监控、数据分析（台风预警、人员风险概览等）。**注意**：离线单文件版（`客舱小助手（离线完整版）.html`）不含本模块。

**操作方法**：

1. 点击「⚠️ 风险预警」页签 → 进入登录页，输入账号密码（管理员账号由团队发放）。
2. 登录后自动刷新实时数据（含真实气象数据），查看顶部预警横幅、风险地图、指标监控与数据分析。
3. 使用完点击 🚪 退出。

**测试账号**（开发/演示环境可用）：账号 `028981` / 密码 `LWH`。正式使用以团队发放账号为准。

### 7️⃣ ❓ 日常问题（daily.html）——高频问题速查

**功能**：高频业务问题快速检索（如请假、着装、航班调整等日常业务问答）。

**操作方法**：进入后直接搜索或点选问题分类 → 查看标准答案。

### 8️⃣ 📕 手册奖惩（manual.html）——手册与奖惩速查

**功能**：手册要点速查 + 奖惩条款速查（扣分/加分规则）。

**操作方法**：进入后按分类检索「手册要点」或「奖惩条款」，可收藏常用条款。

### 9️⃣ 🗂 事件报告（report.html）——报告流程速查

**功能**：机上事件报告流程速查（什么事件要报、怎么报、交什么材料）。

**操作方法**：进入后查看事件类型 → 对应报告流程 / 模板。

### 🔟 📇 库管理（kb-admin.html）——手册维护工作台（管理员专用）

**功能**（本分公司管理员维护知识库用，乘务员端只消费更新后的知识，看不到管理细节）：

**五大 Tab**：

| Tab               | 用途                                                       |
| ----------------- | -------------------------------------------------------- |
| **上传解析**          | 上传新版手册 PDF → 自动解析章节结构（预检页数/文字密度，疑似扫描件会提示先 OCR）           |
| **覆盖率地图**         | 每库章节覆盖状态：已覆盖✅ / 新增 / 变更 / 缺口，直观展示待补任务量                   |
| **差异审核**          | 新增/变更条目逐条人工审核：补写 Q/A、勾选批量确认、写入覆盖层（可选 AI 生成候选）            |
| **体检报告**          | 存量四库健康度扫描（裸缺口 / 弱覆盖 / 标签质量），可导出 Markdown 汇报              |
| **覆盖层管理 / 数据包中心** | 查看覆盖层条目，导出/导入/清空、复制为 JS 片段；**📦 数据包中心**处理知识包 / 销售包 / 题库包 |

**操作方法**：

1. 点击「📇 库管理」→ 首次进入自动把旧版知识覆盖层迁移为数据包。
2. **更新知识**：上传 PDF（选择目标库 CCM/MGM/SVC/Daily）→ 自动解析 → 查看覆盖率地图 → 差异审核（新增条目补 Q/A 后勾选入库）→ 确认后**乘务员端 qa 问答立即生效**。
3. **更新内容数据**：进入「📦 数据包中心」→ 导入数据包 JSON（`kb` 知识包 / `sales` 销售包 / `quiz` 题库包）→ 导入后 qa·美妆·考核**自动生效**。sales 包导入前自动做合规检查（10 项违禁词）。
4. 也可「导出增量包」发给其他设备导入；「复制为 JS 片段」用于正式回写源码。
5. 顶部出现「📦 新数据包」时，管理员可在这里移除旧包回退基线。

***

## 六、数据包（内容更新机制）

### 数据包类型

| 类型       | 含义                           | 生效模块    |
| -------- | ---------------------------- | ------- |
| `kb`     | 知识库（可限定单库 ccm/mgm/svc/daily） | 💬 你问我答 |
| `sales`  | 美妆产品                         | 💄 美妆话术 |
| `quiz`   | 题库                           | 📚 培训考核 |
| `notice` | 通知（预留）                       | —       |

### 更新流程（全员）

1. App 启动 / 回到前台时自动检查团队发布源 `packs/manifest.json`。
2. 发现新包 → 顶栏出现「📦 新数据包(n)」→ 点击 → 「⬇️ 全部安装」。
3. 安装完成自动生效（qa·美妆·考核），Toast 提示。
4. 回滚：管理员可在「库管理 → 数据包中心」移除任意包，恢复内嵌基线。

### 注意

* 数据包存本机（不随个人备份迁移），换机后重新安装即可。

* 下载走 https + sha256 双重校验；离线 / 网络不可达时静默不打扰。

* 大陆网络受限时自动回源 jsdelivr 镜像，无需手动设置。

***

## 七、常见问题（FAQ）

| 问题               | 解决办法                                                      |
| ---------------- | --------------------------------------------------------- |
| 打开是旧版内容          | 强制刷新：Ctrl + F5（电脑）/ 下拉重载（手机）                              |
| 某个模块一直加载         | 15 秒后会自动切换线路；双线路失败时点击加载页重试；检查网络                           |
| 忘记备份、换手机         | 下次换机前先「💾 备份」导出；备份不包含内容数据包，换机后重新装一次「📦 新数据包」              |
| 上传 PDF 提示“疑似扫描件” | 手册需为文字版 PDF，先做 OCR 再上传                                    |
| AI 问答不可用         | 未配置 AI Key 时自动用内置四库；按页面内教程配置后即可自由问答                       |
| 绩效导入格式报错         | 先在模块内下载导入模板，按模板列名整理（自动识别：乘务长/乘务员/销售/运行/综合/工作评定/安全质量/飞行小时） |
| 风险预警无法登录         | 联系团队获取账号密码（演示账号 028981 / LWH）                             |
| 想彻底离线使用          | 使用「客舱小助手（离线完整版）.html」，双击即用                                |

***

## 八、给管理员的发布维护速查（开发侧）

| 操作        | 命令                                                                                      |
| --------- | --------------------------------------------------------------------------------------- |
| 改十模块源码    | 直接改 `qa.html` 等模块文件（即线上）                                                                |
| 发布数据包     | 包 JSON 放入 `packs/incoming/` → `python _build_packs_release.py` → git 提交推送到 GitHub Pages |
| 重建离线单文件版  | `python _build_4in1.py`（十模块）/ `python _gzip_build.py`（九模块）                              |
| 重建库管理模块   | 改 `kb-admin.template.html` 后 `python _build_kbadmin.py`                                 |
| 一键构建 + 回归 | `python _build_all.py`                                                                  |
| 语法校验      | `python _check_js.py`                                                                   |

> 详细开发文档见 `README.md`、`packs/README.md`、`docs/superpowers/specs/`。

