# 你问我答 · 增强提案清单（2026-09-21）

> 基线：`qa.html`（9.53 MB / 12,5xx 行 / 09-21 14:38 版），六库 + 78 项功能 / 11 组 / 300 函数 / 12 个本地存储键
> 用法：每条有编号（L1、M3…），直接说「做 L1」「N4 改成…」即可，我按编号定位源码。
> 口径：qa 侧零密钥零外发（2026-09-19 决策）——下列所有提案均可在**纯本地**实现。
> 进度：**批 1 已实施 6 项（见下方第〇节）**；剩余 18 项待选。

---

## 〇、已完成（2026-09-21 批 1 · 6 项）

已于本日落地并通过 31 项真实浏览器验证（`_qa_enh_verify_20260921.js`）：

| 编号 | 项目 | 落地形态 |
|---|---|---|
| N3 | 一键复制答案 | 每条回答下方「📋 复制」。复制内容是**可直接转发给同事的答案**：自动剔除形象口播（立春给你翻到了—— / 还有想问的随时喊我。）、推荐芯片段、跳转按钮，保留正文与📚出处 |
| N5 | 答案朗读 | 「🔊 朗读 / ⏹ 停止朗读」。走浏览器本地 `speechSynthesis`（zh-CN 优先），零联网零外发 |
| N1 | 收藏 / 星标 | 答案卡「☆ 收藏 / ★ 已收藏」；控制台「👤 我的 → ★ 收藏」列出全部收藏，**点一条即可重问**，可逐条取消 / 两段式清空。存 `qa_star_v1`（上限 200） |
| M1 | 置信度徽标 | 答案顶部「置信度 高 / 中 / 低」。**用真实命中总分而非二值猜测**：包一层 `kbReply` 抓 `hits[0].s`，悬浮显示「总分 N，来源 X，另有 M 条相近条目」 |
| L5 | 成绩趋势曲线 | 「👤 我的 → 📈 练习」：共 N 次 / 平均分 / 最好分 + 折线趋势（虚线为 60 分及格线）+ 最近 8 次明细。数据源 `qa_train_records_v1` |
| P1 | 字号三档 | 「👤 我的」底部 标准 / 大 / 特大（16 / 17.5 / 19px 根字号），本机记住 |

实现方式（两个幂等补丁块，已登记进构建链与回归）：

- `_apply_qa_answer_tools_20260921.py` → 块 `__QA_ANSWER_TOOLS_20260921__`：CSS + 包装 `addMsg`（工具条与徽标写进 html 字符串，随会话持久化）+ 包装 `kbReply`（捕获命中分）
- `_apply_qa_mypanel_20260921.py` → 块 `__QA_MYPANEL_20260921__`：控制台「👤 我的」键 + 面板（收藏 / 练习 / 字号）
- 两者均 **未修改 `kbReply` / `addMsg` / `renderChat` 的任何一行原实现**，只做外层包装与 DOM 追加

---

## 一、四条洞察（先看这个）

1. **最大的缺口是「学」的闭环，不是「问」的能力。** 现在能把题做出来、当场判分，但**不做完就不会了**：答错即忘，没有错题本、没有复习、没有整卷模拟考。对乘务员来说，「考过」才是真需求。
2. **有一批功能「数据已经算出来了，只是没给用户看」。** `kbStrong` 已算出命中总分、`qa_train_records_v1` 已存了每次成绩与时间戳——置信度徽标、成绩趋势曲线都属于低改动高感知的「捡漏项」。
3. **传播力是空白。** 全模块没有一个「复制答案」「收藏」「分享给同事」的出口。乘务员是班组协作场景，一条好答案的价值在于被转发。
4. **09-19 那份清单的 5 项待处理发现，4 项已经解决或失效**（见文末勘误）——不要按旧清单排期。

---

## 二、L 组 · 学习闭环（把「能答题」变成「记住了」）

| 编号 | 功能 | 是什么 | 为什么值得 | 改动面 / 落点 |
|---|---|---|---|---|
| **L1** | **错题本** | 答错的题自动入库，抽屉里一键「重练错题」 | 学习闭环的地基，L3/L6 都建在它上面 | **中** · `qaGradeQuiz` + `qaTrainQuizHtml`；⚠️ `.tq-item` 目前只有 `data-ans/data-explain`，**没有题目标识**，需补 `data-q`（题干）或题号哈希；新增 `qa_wrong_v1` |
| **L2** | **章节掌握度热力图** | 按十章统计错题密度，输出热力块 / 进度条 | 一眼看出「第三章最弱」 | 中 · 依赖 L1 的错题数据；渲染可复用 `qaPerfCardHtml` 的卡片样式 |
| **L3** | **间隔重复复习（SRS）** | 错题按 1/3/7/15 天自动排期，进页面时弹「今日复习 N 题」 | 把「刷过」变成「记住」，是乘务员真正的痛点 | 中 · 本地队列 + 可配 automation 定时提醒 |
| **L4** | **整卷模拟考** | 限时整卷（如 40 题）+ 交卷统一评分 + 成绩单（分章节正确率） | 现在是「出题即逐题即时判分」，没有考场感 | 中 · `qaTrainQuizHtml` 加 `mode` 参数；复用 `qaGradeQuiz` |
| **L5** | **成绩趋势曲线** | 近 7 天 / 30 天平均分折线 + 最高分 | **数据已经在 `qa_train_records_v1` 里躺着**（含 `at`/`score`），几乎零成本 | **小** · 纯新增渲染，不改现有逻辑 |
| **L6** | **每日一练** | 首屏卡片「今天的 3 道题」，可定时推送 | 培养习惯，日活最便宜的做法 | 小-中 · 复用 `qaPickTrainQuestions`；可挂 automation |

---

## 三、M 组 · 检索可信度（把内部分数暴露给用户）

| 编号 | 功能 | 是什么 | 为什么值得 | 改动面 / 落点 |
|---|---|---|---|---|
| **M1** | **置信度徽标** | 答案顶部标「高 / 中 / 低」，低置信时主动说「我不太确定，以下是最相近的条目」 | 现在只有「强命中直答 / 没命中」二值，中间地带用户无从判断 | **小** · `kbStrong` 已算出总分 `s`，只是没展示 |
| **M2** | **「为什么是这条」面板** | 展开可见：命中词、总分、来源库、同义扩展轨迹 | 主管/培训调库时的取证工具；也是用户信任的来源 | 小-中 · `kbSearch` 返回结构加一层 debug 透出（可只在调试模式显示） |
| **M3** | **原文定位跳转** | 点出处徽标直达手册对应段落（现只标章节号文字） | 从「告诉你出处」到「带你去看」 | 中 · 依赖手册页 `manual.html` 侧有稳定锚点，需先确认 |
| **M4** | **版本时效标注** | 答案标注「基于 管理手册 01.14」，手册换版时提示「该条目已变更」 | 手册改版后旧答案会静默过期，这是合规风险点 | 中 · 需建立条目↔版本的映射 |
| **M5** | **安全条目强化** | 酒测标准 / 撤离口令 / 红线类统一高危样式 + 复述确认交互 | 这类答错代价最高，值得差异对待 | 小 · 按 `cat` 打标 + 一套 CSS |

---

## 四、N 组 · 交互与传播（让好答案流动起来）

| 编号 | 功能 | 是什么 | 为什么值得 | 改动面 / 落点 |
|---|---|---|---|---|
| **N1** | **收藏 / 星标** | 一键收藏条目与答案，抽屉里「我的收藏」 | 常用条目（病假流程、酒测标准）反复查 | **小** · 完全复用 `qa_assoc_v1` 的存储模式，新增 `qa_star_v1` |
| **N2** | **分享问答卡** | 把一条问答生成图文卡（问答 + 出处 + CC 形象），图片/文本导出到班组群 | 现在的传播只能靠截图，卡片更体面也更准确 | 中 · canvas 绘制 + `navigator.share` 降级下载 |
| **N3** | **一键复制答案** | 答案卡加复制键 | **全模块只有话术稿能复制**（`copyScript`），知识答案不能 | **极小** · 复用 `fallbackCopy` |
| **N4** | **统一命令面板** | 现只有 `/风格` `/大撤`；扩展 `/练习` `/绩效` `/天气` `/话术` `/查` + 输入时自动补全下拉 | 老用户的手速入口，也顺便解决「不知道能问什么」 | 小-中 · `process()` 顶部 `styleHit` 同款模式 |
| **N5** | **答案朗读** | 一键把答案读出来（浏览器本地 TTS，不联网） | 手上忙着整理机供品时也能听 | **小** · 现 `speechSynthesis` 只用于形象气泡口播且默认关，答案朗读是新增 |

---

## 五、O 组 · 个人化与主管视图

| 编号 | 功能 | 是什么 | 为什么值得 | 改动面 / 落点 |
|---|---|---|---|---|
| **O1** | **岗位化答案详略** | 个人资料加「岗位」（乘务长 / 3 号位 / 见习），答案按岗位裁剪或补充 | 同一条规章，乘务长和见习关心的部分不一样 | 中 · 依赖 `user_profile` 扩展 + 各库条目标岗位标签 |
| **O2** | **我的成长档案** | 练习记录 + 错题 + 提问历史汇总成一页，可导出 | 个人复盘 + 晋级答辩素材 | 小-中 · 数据源全是本地已有的 |
| **O3** | **反馈看板（主管视角）** | 把 F 组反馈聚合成「最常被吐槽的问题 Top10 / 最常被改写的问法」 | 现在反馈只是单条记录，没有汇总面 | 中 · `cabin_qa_feedback_v1` 已有数据，缺聚合视图 |
| **O4** | **知识库缺口清单** | 零命中问法聚类，输出「该补进库的问题」 | 让知识库自己长出该有的条目 | 中 · 需记录零命中问法（现在没留存） |

---

## 六、P 组 · 体验 / 无障碍 / 离线

| 编号 | 功能 | 是什么 | 为什么值得 | 改动面 / 落点 |
|---|---|---|---|---|
| **P1** | **字号三档** | 标准 / 大 / 特大 | 客舱环境 + 年龄跨度，现在只有 44px 触控达标 | **小** · 一组 CSS 变量 + 一档开关 |
| **P2** | **离线状态条 + 离线包** | 明确提示「当前离线，仍可查六库」；PWA 侧预缓存六库 | 现在断网后行为不透明，用户会以为坏了 | 中 · SW 缓存策略 |
| **P3** | **平板 / 横屏重排** | 宽屏下左库导航 + 右答案，充分利用空间 | 三栏下限 761px 已做，平板端仍有空间浪费 | 中 · ⚠️ `petArtFit` 的 tri 阈值与 CSS 必须同步改 |
| **P4** | **新答案播报** | 结果区 `aria-live` 播报（现只标了 `aria-busy`） | 屏幕阅读器用户唯一的可达路径 | **极小** · 一处属性 |

---

## 七、Q 组 · 我建议**不要**做（附理由）

| 编号 | 想法 | 不建议的原因 |
|---|---|---|
| **Q1** | 回归云端大模型直连 | 违背 2026-09-19「源码零密钥、运行时零外发」口径，且会推翻四条守护链的断言。**若要做**，唯一合规姿势是照 beauty 的 `__AI_HYBRID_20260919__` 例外：运行时自填 Key、可选、失败即回落本地 |
| **Q2** | 拍照 / 图片提问 | 本地无 OCR 引擎 → 必然外发，直接排除 |
| **Q3** | iOS 语音输入 | iOS 无本地 ASR，只能引导用 Safari（现有 `__QA_IOS_MIC_v1__` 已是上限） |
| **Q4** | 排班 / 执勤期 / 航段时间查询 | 无合规数据来源，属于「看起来很美但落地即违规」 |

---

## 八、建议的起手 8 条（性价比排序）

| 序号 | 编号 | 项目 | 理由 |
|---|---|---|---|
| 1 | L5 | 成绩趋势曲线 | 数据已有，纯新增渲染 |
| 2 | M1 | 置信度徽标 | 分数已有，只差展示 |
| 3 | N3 | 一键复制答案 | 改动最小，使用频率最高 |
| 4 | N1 | 收藏 / 星标 | 复用联想记忆的存储模式 |
| 5 | P1 | 字号三档 | 覆盖最广的体验改善 |
| 6 | N5 | 答案朗读 | 本地 TTS，零外发 |
| 7 | L1 | **错题本** | 唯一需要动题卡模板的，但价值最高 |
| 8 | L6 | 每日一练 | 复用现有出题能力 |

> 建议顺序：先做 1-6（互不依赖、各自独立可上线），再做 7-8（成对，L6 依赖 L1 的数据结构）。

---

## 九、基线勘误（09-19 清单里的 5 项待处理发现）

| 原发现 | 现状 |
|---|---|
| 欢迎卡与快捷提问不显示 | **属既定产品决策**（`QA_SHOW_WELCOME=false`，用户要求不自动弹）——不是 bug。若要恢复入口，建议改成底部控制台常驻芯片而非首屏弹卡 |
| CCPiano 代码块重复 6 份 | **已修复**，现在源码中恰好 1 份 |
| 未命中卡缺 CBT 切库按钮 | **已修复**，现已含 CBT |
| 内置共享 AI Key 明文 | **已失效**——AI 已整体下线，Key 已随之下线 |
| 使用说明已过期 | **仍待办** · 《产品使用清单与操作方法.md》仍写「四库问答」，未覆盖六库 / 大撤 / CBT / 绩效查询 / 在线练习 / 联想记忆 |

**另外该清单的 C 组（AI 能力，9 项）整体描述已过期**——现在是「无 AI 降级模式」为默认态，C1-C7 应重写。

---

*清单由源码实测生成 · 覆盖 qa.html 全部函数 / 元素 id / 事件绑定 / 本地存储键 · 2026-09-21*
