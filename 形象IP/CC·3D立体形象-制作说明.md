# CC·3D 立体形象 — 制作说明

> 春秋航空 · 客舱小助手｜24 节气形态 CC 真 3D 模型制作全记录
> 状态：**✅ 24/24 全部完成**（cc01–cc24，2026-09-19 收官），已执行 `_finalize_3d.py` 收尾

---

## 一、产出物

| 产物 | 路径 | 说明 |
|---|---|---|
| 3D 模型 | `形象IP/models/ccNN.glb` | 24 个齐全，每个约 0.23–0.77 MB（已压缩） |
| 预览图 | `形象IP/models/ccNN-preview.png` | 服务端渲染的转台预览 |
| 前端清单 | `_pet_models.json` | qa / cc-home 3D 层读取的模型索引 |
| 任务状态 | `_3d_jobs.json` | 断点续跑依据（done / pending） |
| 运行日志 | `_3d_worker.log` | 工作器全程日志 |

对应关系：ccNN ← 24 节气形态之一，源立绘清单见 `_pet_sprites.json`（cc01=立春 … cc24=大寒）。

## 二、制作管线

1. **提交**：以 2D 立绘为输入，调用混元 hy-3d 图生 3D 服务（generate-type = LowPoly，面数 20000）；
2. **轮询**：单任务约 10–25 分钟出模型；
3. **下载**：GLB 原始体积约 13–17 MB；
4. **贴图压缩**：`_glb_shrink.py` 把 4096² PNG 贴图降为 1024² JPEG → 体积降到约 3%（0.4–0.7 MB）；
5. **base64 包装**：`_glb_pack.py` 生成 file:// 场景可直接内嵌加载的包装；
6. **登记**：写入 `_pet_models.json`，前端 3D 层即可加载该形态。

## 三、服务硬限制（为什么分多天跑）

- **每日最多提交 5 次**（hy-3d 维度配额）；
- **最多 2 个并发任务**（超出返回 429）。

因此 24 个模型需 **5 天左右**排队完成，每天由常驻工作器 `_3d_worker.py` 自动跑一轮：

```
echo -n "<token>" | python _3d_worker.py --reset --slots 2 --max-minutes 400
```

- `--reset`：把此前因每日额度停下的任务重新排队（跨天续跑）；
- 撞到每日额度时工作器**干净退出**，剩余任务保持 pending，次日额度重置后继续；
- 每轮结束自动重写 `_pet_models.json`，已完成模型即时可用。

## 四、前端接入

- qa / cc-home 形象条：**有 GLB 的形态**点击可开关 3D 展示；**无模型的形态**点击会提示「模型还没生成好（x/24）」，不误触；
- 3D 层由 base64 包装内嵌加载，无外部网络依赖，file:// 直开可用；
- 24 个 **已于 2026-09-19 全部完成后执行** `python _finalize_3d.py` 收尾：重建 qa.html / cc-home.html 3D 层 → `_build_all.py`（16 文件语法 + 全套回归）→ `_build_hosted.py`（在线版同步 models，24 个 GLB 已进 `在线版/形象IP/models/`）→ `_check_needles.py`（23 项检查 0 fail）；全部 exit=0。
- **2026-09-21 再次收尾**（破损重制 12 个全部落盘后重跑）：`_glb_pack` → `_build_home`（cc-home.html 7.36 MB，3D 24/24）→ `_apply_pet --replace`（qa.html 3D 24/24）→ `_build_all`（**16 文件语法全过**）→ `_build_hosted` → `_check_needles`（**42 checked / 0 fail**，含 `在线版/形象IP/models/cc*.glb(24)` 资产校验）；重制后的模型已全部进入 `cc-home.html` + `qa.html` + `在线版/`。

## 五、进度台账

| 批次 | 日期 | 新增 | 累计 |
|---|---|---|---|
| 1 | 2026-09-14 | cc01–cc04 | 4/24 |
| 2 | 2026-09-15 | cc05–cc09 | 9/24 |
| 3（返工+新） | 2026-09-16 | cc07、cc08、cc09 返工重做 + cc10、cc11 新增 | **11/24** |
| 4 | 2026-09-17 | cc12–cc16 | **16/24** |
| 5 | 2026-09-18 | cc17–cc21 | **21/24** |
| 6（收官） | 2026-09-19 | cc22–cc24 | **✅ 24/24** |

### 批次 3 明细（2026-09-16 10:10–10:49）

- 提交 5 次（07→11，用满当日配额），**5/5 全部成功，真失败 0**；
- 成品体积：cc07 0.41 MB（原始 14.8）、cc08 0.57 MB（17.6）、cc09 0.36 MB（12.6）、cc10 0.36 MB（12.8）、cc11 0.45 MB（14.1）——压缩率稳定在 3% 左右；
- 返工项 cc07/08/09 已按透明底抠图输入集重跑（源 `形象IP/cutouts/3d-ccNN.png`），待目检确认头部/发型问题是否消除；
- 撞到 daily 5/5 上限后工作器干净退出，cc12–cc24 共 13 个保持 pending，次日加 `--reset` 续跑即可。

## 六、输入源规范（2026-09-15 定稿，全量 24 角色适用）

- **教训**：首批提交时 `find_src` 按目录序命中 `原稿/`（带全场景背景+水印，RGB 无 alpha）——复杂背景/道具（椅子、行李箱、夜景飞机）会干扰重建：cc07 头部塌陷破洞、cc09 头发糊块。审计确认该问题波及全部 22 个未返工角色；cc24 的原始立绘在项目外，`find_src` 直接缺失。
- **统一规范（已落地）**：3D 专用输入集 = `形象IP/cutouts/3d-ccNN.png`（24 张全套）——透明底 RGBA 主体抠图、无背景无水印、原生分辨率零降采样（22 张 1024×1536，cc02 864×1152 原生，cc24 1486×1981）。`_3d_jobs.json` 全部 24 个作业的 src 已统一指向该命名（唯一命中，杜绝目录序歧义）。
- **健康度目检**：Q 版系列 cc01–cc06 全部健康（脸/道具/构图重建良好——Q 版构图简单，背景干扰小）；正常头身比系列 cc07 ✕、cc08 ✕、cc09 ✕ 已于 2026-09-16 用透明底抠图重跑完毕；cc10–cc16 均为统一输入集（透明底抠图）产出，待目检复核。
- **单角色返工流程**：把 `cutouts/ccNN.png`（或修好的新图）复制为 `3d-ccNN.png` 覆盖 → `_3d_jobs.json` 该角色置 pending、jobId 清空 → 次日 10:05 批次自动优先跑（pending 按编号升序提交）。返工消耗每日 5 次额度中的名额。
- 模型贴图固定压缩为 1024² JPEG（体积 ≈0.5MB/个）；qa / cc-home 展示尺寸下清晰度足够，如需 4K 级贴图再议（体积约涨 3–4 倍）。

### 批次 4 明细（2026-09-17 10:13–10:47）

- 提交 5 次（12→16，用满当日配额），**5/5 全部成功，真失败 0**，全程 33 分钟；
- 成品体积：cc12 0.69 MB、cc13 0.44 MB、cc14 0.37 MB、cc15 0.38 MB（原始 14.4）、cc16 0.53 MB（原始 13.1）——压缩率稳定在 3% 左右；
- 撞到 daily 5/5 上限后工作器干净退出，cc17–cc24 共 8 个保持 pending，次日加 `--reset` 续跑即可；
- **预计 09-18 跑 cc17–cc21（5 个）、09-19 跑 cc22–cc24（3 个）并触发 `_finalize_3d.py` 收尾**。

> 2026-09-18 由自动化工作器生成并更新

### 批次 5 明细（2026-09-18 10:10–10:41）

- 提交 5 次（17→21，用满当日配额），**5/5 全部成功，真失败 0**，全程 31 分钟；
- 成品体积：cc17 0.53 MB、cc18 0.40 MB、cc19 0.47 MB（原始 12.7）、cc20 0.50 MB（原始 15.0）、cc21 0.49 MB（原始 13.8）——压缩率稳定在 3% 左右；
- 撞到 daily 5/5 上限后工作器干净退出，cc22–cc24 共 3 个保持 pending；
- 剩余最后一轮：次日加 `--reset` 续跑 cc22–cc24，跑完即 24/24 并触发 `_finalize_3d.py` 收尾。

### 批次 6 明细（2026-09-19 10:13–10:36，收官）

- 提交 3 次（22→24，未用满当日 5 次配额），**3/3 全部成功，真失败 0**，全程 22 分钟；
- 成品体积：cc22 0.48 MB（原始 15.0）、cc23 0.23 MB（原始 11.1）、cc24 0.74 MB（原始 17.4，本项目最大立绘 1486×1981）——压缩率稳定在 3% 左右；
- 24/24 全部完成，`_3d_jobs.json` 无 pending，工作器自动干净退出（日志末行「结束：24 完成 / 0 失败 / 0 排队」）；
- **已执行收尾 `_finalize_3d.py`**（exit=0，27 秒）：`_glb_pack.py` 打包 base64 包装（24 个全就绪）→ `_build_home.py`（cc-home.html 7.35 MB，3D 层 24/24 已注入）→ `_apply_pet.py`（qa 形象条 24/24）→ `_build_all.py`（16 文件语法 + 回归，exit=0）→ `_build_hosted.py`（在线版，exit=0）→ `_check_needles.py`（23 项检查 **0 fail**，在线版 `形象IP/models/cc*.glb` 24 个资产校验通过）；
- 至此 24 节气形态 CC 全部具备真 3D 立体形象，前端「有模型的形态可开关 3D」逻辑对全部 24 个形态生效。

> 2026-09-19 由自动化工作器生成并更新 · 3D 形象制作收官



---

## 七、破损重制（2026-09-20）

背景：9-19 完成 24/24 后目检发现 12 个存在重建缺陷（07/08/09/10/12/13/14/15/17/18/22/23，以 22 立秋、23 大雪的头脸破损最重；10 头部有纸片碎片）。按「重抠透明底源 → 逐个重掷」推进，重制消耗同一份每日 5 次配额。

| 重制批次 | 日期时间 | 提交 | 结果 |
|---|---|---|---|
| R1 | 2026-09-20 00:16–00:45 | cc07、cc08、cc09、cc10、cc12 | 5/5 下载成功，真失败 0 |
| R2 | 2026-09-20 13:22–13:52 | cc13、cc14、cc15、cc22、cc23 | 5/5 全部成功，真失败 0，全程 35 分钟 |
| R3（收官） | 2026-09-21 13:29–13:42 | cc17、cc18 | 2/2 成功，真失败 0，全程 12 分钟 —— **24/24 全绿并完成收尾** |
| R4（目检重掷） | 2026-09-21 14:11–14:22 | cc08、cc10 | 2/2 成功，真失败 0，全程 11 分钟；**cc10 先修源图**再重掷 |

### R1 明细（2026-09-20 00:16–00:45）

- 5 次提交全部跑完并下载到 GLB，但落盘压缩阶段报 `No module named 'PIL'`（当时 envs/default 未装 Pillow），**GLB 未落盘、体积记为 0.00 MB**；
- 因 jobId 仍在，按 `_salvage_3d_20260920.py` 的 status → 下载 → shrink 流程**零额度抢救**成功（00:50–00:52 补齐）；
- 抢救后体积：cc07 0.36 MB、cc08 0.55 MB、cc09 0.43 MB、cc10 0.60 MB、cc12 0.58 MB；
- 目检：cc07/09/12 明显改善；cc08 脸部仍有涂抹；**cc10 头部仍有纸片碎片，待重掷（排在 09-22 批次）**。

### R2 明细（2026-09-20 13:22–13:52，本批次）

- 5 次提交（13→15、22→23），**5/5 全部成功，真失败 0**，全程 35 分钟；
- 成品体积：cc13 0.45 MB（原始 13.1）、cc14 0.40 MB（13.2）、cc15 0.44 MB（12.9）、cc22 0.48 MB（13.5）、cc23 0.50 MB（14.2 MB）——压缩率稳定 ~3%，Pillow 12.3.0 压缩链路正常；
- 工作器在 22/24 完成后已无待跑作业，手动结束空转（状态与 `_pet_models.json` 早已落盘）；
- **`_3d_jobs.json` 现状：done 22 / hold 2（cc17、cc18）/ pending 0 / fail 0**；`_pet_models.json` 24 条齐全（含 24 个 GLB，无缺失、无异常小文件）。

### R3 明细（2026-09-21 13:29–13:42，收官）

- 起跑 `done 22 / hold 2（cc17、cc18）/ pending 0 / fail 0`。`hold` 是 09-20 人工置位（防止在额度已满的批次里被自动提交），而工作器 `--reset` 只认 `fail` 态 → 先手工把 17/18 退回 `pending`（`_requeue_3d_20260921.py`，原子写 + 复验），再交工作器提交；
- 2 次提交（13:29:50 / 13:29:51），当日配额仅用 2/5（未撞限额），**全程 12 分钟干净退出**：cc18 0.40 MB（原始 17.0 MB）、cc17 0.65 MB（原始 16.3 MB）；
- 结束日志：`结束：24 完成 / 0 失败 / 0 排队`；`_3d_jobs.json` = **done 24 / pending 0 / fail 0**；`_pet_models.json` 24 条齐全；
- 输入源为 09-20 重抠的透明底 `形象IP/cutouts/3d-cc17.png`、`3d-cc18.png`（09-20 00:15 / 00:16 生成），`find_src` 唯一命中。

### 收尾结果（2026-09-21）

- `_finalize_3d.py` 已执行：`_glb_pack` exit=0（ready.js 覆盖 1–24）→ `_build_home` exit=0（3D 24/24，cc-home.html 7.36 MB）→ `_apply_pet --replace` exit=0（3D 24/24）→ `_build_all` exit=1（**16 文件语法全过**；唯一失败套件 = APK `assets/www` e2e 的 2 条 qa 首屏断言，与 09-20 基线逐字一致、属既存问题——APK 的 www 副本只在打包时刷新）→ `_build_hosted` exit=0（24 个 GLB + js 已同步 `在线版/形象IP/models/`）→ `_check_needles` exit=0（**42 checked / 0 fail**，含 24 GLB 资产校验）；
- **3D 形象主线完结：24/24 重制版已全部进入 `cc-home.html` / `qa.html` / 在线版**。APK 与 PWA 仍待用户明确通知后打包（届时自然带上新 3D 层）。

### R4 明细（2026-09-21 14:11–14:22，目检重掷 cc08 / cc10）

用户指令重掷 R1 后目检仍不满意的两个：**cc10 头部「纸片」**、**cc08 脸部涂抹**。当日配额 2/5 已用，本次再用 2 次（累计 4/5，未撞限额）。

- **cc10 是源图问题，不是模型问题** —— 逐像素定位后确认：`形象IP/cutouts/3d-cc10.png` 沿发丝轮廓散落着若干**宽 1~3px 的半透明灰蓝虚线状残留**（原图抗锯齿边缘没被抠干净），3D 服务把这种细长结构重建成平面薄片，于是预览里头顶出现「纸片」。R1 用的就是这张源图，**单纯重掷必然复现**，故先修源：
  - `_cc10_fix_20260921.py` = 形态学开运算（2 次腐蚀 → 保最大连通域 → 2 次膨胀，等效 5×5 核）。细丝被断开清除，主体（厚度远超 5px）原样保留；
  - 前景像素 751,522 → 730,677（**减少 2.77%**），腐蚀后 61 个连通域收敛为单一主体；旧图备份 `_bak_3d_redo_20260921/3d-cc10.before_clean.png`；
  - 品红底放大目检确认：轮廓虚线状残留全部消失，发型主体无损。
- **cc08 是模型重建问题**：源图（滑雪服+雪镜+雪杖+雪板）本身干净、脸部绘制正常，面部涂抹属服务侧重建噪声 → 直接重掷。
- 结果：cc08 0.56 MB（原始 17.7 MB）、cc10 0.53 MB（原始 13.5 MB）；`结束：24 完成 / 0 失败 / 0 排队`。
- 重掷后再次收尾：`_glb_pack` / `_build_home` / `_apply_pet` / `_build_hosted` / `_check_needles` 全 exit=0（**42 checked / 0 fail**）；`_build_all` 仍 exit=1，语法 **16/16 全过**，唯一失败套件 `_e2e_apk_www_test.js` **27/29**，2 条断言与 09-20 基线逐字一致（既存，APK www 副本未动）。
- 一致性核验：`形象IP/models/cc08.glb`(14:21) → `js/cc08.js`(14:28) → `cc-home.html`(14:28) → `在线版/`(14:31)，**md5 源与在线版完全一致**；cc10 同理。重掷版已进入全部交付物。

### 运维经验

1. **配额日界不是本地 0 点**：09-20 00:16 已用满 5/5，同日 13:22 又能提交 5 次 → 更接近 UTC 日界（约北京 08:00 后刷新），故「白天补跑」同样能拿到全新额度；09-21 13:29 再提交 2 次也直接通过。
2. **`_3d_token.txt` 曾遮蔽 stdin 新令牌**：已由 `__TOKEN_STDIN_WINS_20260920__` 修好（stdin 管道有令牌则优先并粘住首读值，无管道才回退文件），本次长跑全程正常。
3. **`hold` 态需手工退回**：`--reset` 只重排 `fail`；人工 `hold` 的作业必须显式改回 `pending`，否则工作器无作业可跑，只会空转到 `max-minutes`。
4. **3D 缺陷先判「源图问题」还是「模型问题」再决定重掷**：把源图裁开放大目检（品红底铺透明区最易看穿），凡是源图里就存在的细长/半透明结构（毛边、虚线状抗锯齿残留、细小道具），重掷只会复现，必须先修源。R4 的 cc10 正是此例——修源后再掷一次即解决。

> 2026-09-21 由自动化工作器生成并更新 · **3D 形象 24/24 重制完成并已收尾**，cc08/cc10 目检重掷已并入
