# CC·3D 立体形象 — 制作说明

> 春秋航空 · 客舱小助手｜24 节气形态 CC 真 3D 模型制作全记录
> 状态：**9/24 已完成**（cc01–cc09），15 个排队中，本文档随进度更新

---

## 一、产出物

| 产物 | 路径 | 说明 |
|---|---|---|
| 3D 模型 | `形象IP/models/ccNN.glb` | 每个约 0.44–0.68 MB（已压缩） |
| 预览图 | `形象IP/models/ccNN-preview.png` | 服务端渲染的转台预览 |
| 前端清单 | `_pet_models.json` | qa / cc-home 3D 层读取的模型索引 |
| 任务状态 | `_3d_jobs.json` | 断点续跑依据（done / pending） |
| 运行日志 | `_3d_worker.log` | 工作器全程日志 |

对应关系：ccNN ← 24 节气形态之一，源立绘清单见 `_pet_sprites.json`（cc01=立春 … cc24=大寒）。

## 二、制作管线

1. **提交**：以 2D 立绘为输入，调用混元 hy-3d 图生 3D 服务（generate-type = LowPoly，面数 20000）；
2. **轮询**：单任务约 10–25 分钟出模型；
3. **下载**：GLB 原始体积约 13–17 MB；
4. **贴图压缩**：`_glb_shrink.py` 把 4096² PNG 贴图降为 1024² JPEG → 体积降到约 3%（0.4–0.7 MB）；
5. **base64 包装**：`_glb_pack.py` 生成 file:// 场景可直接内嵌加载的包装；
6. **登记**：写入 `_pet_models.json`，前端 3D 层即可加载该形态。

## 三、服务硬限制（为什么分多天跑）

- **每日最多提交 5 次**（hy-3d 维度配额）；
- **最多 2 个并发任务**（超出返回 429）。

因此 24 个模型需 **5 天左右**排队完成，每天由常驻工作器 `_3d_worker.py` 自动跑一轮：

```
echo -n "<token>" | python _3d_worker.py --reset --slots 2 --max-minutes 400
```

- `--reset`：把此前因每日额度停下的任务重新排队（跨天续跑）；
- 撞到每日额度时工作器**干净退出**，剩余任务保持 pending，次日额度重置后继续；
- 每轮结束自动重写 `_pet_models.json`，已完成模型即时可用。

## 四、前端接入

- qa / cc-home 形象条：**有 GLB 的形态**点击可开关 3D 展示；**无模型的形态**点击会提示「模型还没生成好（x/24）」，不误触；
- 3D 层由 base64 包装内嵌加载，无外部网络依赖，file:// 直开可用；
- 24 个全部完成后运行 `python _finalize_3d.py` 收尾：重建 qa.html / cc-home.html 3D 层 → `_build_all.py`（16 文件语法 + 全套回归）→ `_build_hosted.py`（在线版同步 models）→ `_check_needles.py`（21/21）。

## 五、进度台账

| 批次 | 日期 | 新增 | 累计 |
|---|---|---|---|
| 1 | 2026-09-14 | cc01–cc04 | 4/24 |
| 2 | 2026-09-15 | cc05–cc09 | **9/24** |
| 3（返工+新） | 09-16 | cc07、cc08、cc09 返工 + cc10–cc11 | → 11/24 |
| 待续 | 09-17 起 | cc12–cc24（每日 5 个） | → 24/24 |

## 六、输入源规范（2026-09-15 定稿，全量 24 角色适用）

- **教训**：首批提交时 `find_src` 按目录序命中 `原稿/`（带全场景背景+水印，RGB 无 alpha）——复杂背景/道具（椅子、行李箱、夜景飞机）会干扰重建：cc07 头部塌陷破洞、cc09 头发糊块。审计确认该问题波及全部 22 个未返工角色；cc24 的原始立绘在项目外，`find_src` 直接缺失。
- **统一规范（已落地）**：3D 专用输入集 = `形象IP/cutouts/3d-ccNN.png`（24 张全套）——透明底 RGBA 主体抠图、无背景无水印、原生分辨率零降采样（22 张 1024×1536，cc02 864×1152 原生，cc24 1486×1981）。`_3d_jobs.json` 全部 24 个作业的 src 已统一指向该命名（唯一命中，杜绝目录序歧义）。
- **健康度目检（已完成 9 个）**：Q 版系列 cc01–cc06 全部健康（脸/道具/构图重建良好——Q 版构图简单，背景干扰小）；正常头身比系列 cc07 ✕、cc08 ✕、cc09 ✕ 均已返工排队（08 侧脸偏白，用户决定一并重做）。
- **单角色返工流程**：把 `cutouts/ccNN.png`（或修好的新图）复制为 `3d-ccNN.png` 覆盖 → `_3d_jobs.json` 该角色置 pending、jobId 清空 → 次日 10:05 批次自动优先跑（pending 按编号升序提交）。返工消耗每日 5 次额度中的名额。
- 模型贴图固定压缩为 1024² JPEG（体积 ≈0.5MB/个）；qa / cc-home 展示尺寸下清晰度足够，如需 4K 级贴图再议（体积约涨 3–4 倍）。

> 2026-09-15 由自动化工作器生成并更新
